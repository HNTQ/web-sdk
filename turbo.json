{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "cache": true,
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@authdog/types#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**"]
    },
    "@authdog/shared#build": {
      "dependsOn": ["^build", "@authdog/types#build"],
      "outputs": ["dist/**", "build/**"]
    },
    "@authdog/sdk-browser#build": {
      "dependsOn": ["^build", "@authdog/shared#build"],
      "outputs": ["dist/**", "build/**"]
    },
    "@authdog/authdog-react#build": {
      "dependsOn": ["^build", "@authdog/sdk-browser#build"],
      "outputs": ["dist/**", "build/**"]
    },
    "@authdog/nextjs#build": {
      "dependsOn": ["^build", "@authdog/authdog-react#build"],
      "outputs": ["dist/**", "build/**"]
    },
    "@playground/nextjs": {
      "dependsOn": ["^build", "@authdog/react#build"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "test": {
      "outputs": ["coverage/**"],
      "cache": false
    }
  }
}
